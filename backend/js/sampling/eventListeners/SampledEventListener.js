const EventListener = require("./EventListener");

class SampledEventListener extends EventListener {
    constructor() {
        super();
    }

    handleEvent(event /*SampledEvent*/) {

    }
}

module.exports = SampledEventListener;